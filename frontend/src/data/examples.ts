import type { FlowRecord } from "../api/types";

export interface ExampleSignal {
  feature: string;
  value: string;
  why: string;
}

export interface Example {
  name: string;
  tag: string;
  why_short: string;
  why_full: string;
  signals: ExampleSignal[];
  dst_port_default: number;
  flow: Partial<FlowRecord> & Pick<FlowRecord, "protocol">;
}

export const EXAMPLES: Example[] = [
  {
    name: "BENIGN",
    tag: "baseline · ~70% of real traffic",
    why_short: "balanced rates, normal packet sizes, healthy TCP handshake.",
    why_full:
      "Normal interactive traffic — a moderate stream of mid-sized packets in both directions, an ACK-heavy mix of TCP flags, and no resets. Everything else is measured against this profile; if no feature is anomalous, the trees route the flow into the benign leaf.",
    signals: [
      { feature: "flow_packets_per_s", value: "7", why: "mid-range, consistent with browsing or API calls" },
      { feature: "total_fwd_packets / total_bwd_packets", value: "10 / 8", why: "balanced — client and server take turns" },
      { feature: "fwd_packet_length_mean", value: "~450 B", why: "typical request size, not a flood, not an exfil" },
      { feature: "ack_flag_count", value: "5", why: "healthy three-way handshake plus replies" },
      { feature: "rst_flag_count", value: "0", why: "no failed connections or scanner aborts" },
    ],
    dst_port_default: 443,
    flow: {
      protocol: "TCP",
      flow_duration: 60000,
      total_fwd_packets: 10,
      total_bwd_packets: 8,
      flow_packets_per_s: 7,
      flow_bytes_per_s: 3000,
      fwd_packet_length_max: 800,
      fwd_packet_length_mean: 450,
      bwd_packet_length_max: 900,
      bwd_packet_length_mean: 500,
      flow_iat_mean: 400,
      flow_iat_std: 250,
      fin_flag_count: 1,
      syn_flag_count: 1,
      rst_flag_count: 0,
      psh_flag_count: 2,
      ack_flag_count: 5,
      src_ip: "10.0.0.5",
      dst_ip: "10.0.0.100",
    },
  },
  {
    name: "DDoS",
    tag: "volumetric flood",
    why_short: "huge forward-packet rate, tiny payload, almost no replies — a SYN flood.",
    why_full:
      "Many sources hammer one victim with crafted packets. The forward direction is saturated (thousands of packets per second of small ~120-byte payloads) while the victim cannot keep up, so backward traffic collapses. SYN counts climb because the handshake never completes.",
    signals: [
      { feature: "flow_packets_per_s", value: "1800", why: "two orders of magnitude above benign" },
      { feature: "flow_bytes_per_s", value: "160 000", why: "very high byte-rate from many tiny packets" },
      { feature: "total_fwd_packets", value: "90", why: "while bwd is only 2 — extreme asymmetry" },
      { feature: "fwd_packet_length_mean", value: "~110 B", why: "small, crafted — not real payloads" },
      { feature: "syn_flag_count", value: "3", why: "SYN-heavy because connections never close" },
      { feature: "flow_duration", value: "33 μs", why: "flow torn down almost instantly" },
    ],
    dst_port_default: 80,
    flow: {
      protocol: "TCP",
      flow_duration: 33,
      total_fwd_packets: 90,
      total_bwd_packets: 2,
      flow_packets_per_s: 1800,
      flow_bytes_per_s: 160000,
      fwd_packet_length_max: 130,
      fwd_packet_length_mean: 110,
      bwd_packet_length_max: 90,
      bwd_packet_length_mean: 60,
      flow_iat_mean: 0.4,
      flow_iat_std: 0.2,
      fin_flag_count: 0,
      syn_flag_count: 3,
      rst_flag_count: 1,
      psh_flag_count: 0,
      ack_flag_count: 4,
      src_ip: "203.0.113.42",
      dst_ip: "10.0.0.100",
    },
  },
  {
    name: "DoS",
    tag: "single-source slow flood",
    why_short: "DDoS-like asymmetry but lower rate and a much longer-lived flow.",
    why_full:
      "Same intent as a DDoS but driven from one origin (slowloris, RUDY, HTTP-flood). The packet- and byte-rate are noticeably lower than DDoS, the flow lingers for hundreds of milliseconds, but the SYN-heavy asymmetry between forward and backward directions still gives it away.",
    signals: [
      { feature: "flow_duration", value: "400 μs", why: "the attacker holds the connection open" },
      { feature: "flow_packets_per_s", value: "720", why: "high, but ~½ of a DDoS" },
      { feature: "flow_bytes_per_s", value: "64 000", why: "matches the slower cadence" },
      { feature: "total_fwd / total_bwd", value: "90 / 2", why: "still extreme asymmetry" },
      { feature: "syn_flag_count", value: "2", why: "incomplete handshakes" },
    ],
    dst_port_default: 80,
    flow: {
      protocol: "TCP",
      flow_duration: 400,
      total_fwd_packets: 90,
      total_bwd_packets: 2,
      flow_packets_per_s: 720,
      flow_bytes_per_s: 64000,
      fwd_packet_length_max: 130,
      fwd_packet_length_mean: 110,
      bwd_packet_length_max: 90,
      bwd_packet_length_mean: 60,
      flow_iat_mean: 1.0,
      flow_iat_std: 0.3,
      fin_flag_count: 0,
      syn_flag_count: 2,
      rst_flag_count: 1,
      psh_flag_count: 0,
      ack_flag_count: 4,
      src_ip: "198.51.100.7",
      dst_ip: "10.0.0.100",
    },
  },
  {
    name: "PortScan",
    tag: "reconnaissance",
    why_short: "ultra-short flow, one-or-two tiny packets, SYN + RST, no payload.",
    why_full:
      "An attacker tickles a port to see whether it's open. The flow lives for microseconds, carries a single 60-byte SYN packet, often gets an immediate RST back from a closed port, and the packets-per-second rate is unreasonably high relative to the total byte count.",
    signals: [
      { feature: "flow_duration", value: "55 μs", why: "barely a handshake" },
      { feature: "total_fwd_packets", value: "2", why: "and total_bwd is 0–1" },
      { feature: "fwd_packet_length_mean", value: "60 B", why: "SYN only — no payload" },
      { feature: "syn_flag_count", value: "2", why: "and rst_flag_count is also high" },
      { feature: "flow_packets_per_s", value: "245", why: "rate inflated by tiny packets" },
    ],
    dst_port_default: 3389,
    flow: {
      protocol: "TCP",
      flow_duration: 55,
      total_fwd_packets: 2,
      total_bwd_packets: 0,
      flow_packets_per_s: 245,
      flow_bytes_per_s: 1100,
      fwd_packet_length_max: 60,
      fwd_packet_length_mean: 60,
      bwd_packet_length_max: 0,
      bwd_packet_length_mean: 0,
      flow_iat_mean: 2.7,
      flow_iat_std: 1.6,
      fin_flag_count: 0,
      syn_flag_count: 2,
      rst_flag_count: 1,
      psh_flag_count: 0,
      ack_flag_count: 0,
      src_ip: "192.0.2.55",
      dst_ip: "10.0.0.100",
    },
  },
  {
    name: "BruteForce",
    tag: "credential abuse",
    why_short: "many short request/response cycles with abnormal RST + PSH flag counts.",
    why_full:
      "Repeated login attempts against SSH, RDP, or a web form. The flow is long enough to fit dozens of failed authentication cycles; each failure shows up as a RST, and the per-cycle PSH/ACK pattern is heavier than a normal session of the same length.",
    signals: [
      { feature: "flow_duration", value: "2980 μs", why: "long enough to chain failed attempts" },
      { feature: "rst_flag_count", value: "1+", why: "repeated failed authentications" },
      { feature: "psh_flag_count", value: "3", why: "each attempt pushes a new credential" },
      { feature: "ack_flag_count", value: "8", why: "many short turn-arounds" },
      { feature: "dst_port", value: "22 (SSH)", why: "or 3389 RDP / 80 web — high-value targets" },
    ],
    dst_port_default: 22,
    flow: {
      protocol: "TCP",
      flow_duration: 2980,
      total_fwd_packets: 20,
      total_bwd_packets: 16,
      flow_packets_per_s: 33,
      flow_bytes_per_s: 1800,
      fwd_packet_length_max: 300,
      fwd_packet_length_mean: 120,
      bwd_packet_length_max: 240,
      bwd_packet_length_mean: 100,
      flow_iat_mean: 150,
      flow_iat_std: 90,
      fin_flag_count: 1,
      syn_flag_count: 2,
      rst_flag_count: 1,
      psh_flag_count: 3,
      ack_flag_count: 8,
      src_ip: "198.51.100.99",
      dst_ip: "10.0.0.50",
    },
  },
  {
    name: "WebAttack",
    tag: "L7 injection",
    why_short: "looks like benign HTTP except the forward payload is huge and PSH-heavy.",
    why_full:
      "SQLi, XSS, command-injection. Packet counts and TCP flags look ordinary, but the forward direction carries unusually long URLs or encoded payloads — fwd_packet_length_max sits at the MTU ceiling and PSH counts climb because the attacker is shoving data.",
    signals: [
      { feature: "fwd_packet_length_max", value: "1200 B", why: "near MTU — packed URL or body" },
      { feature: "total_length_fwd_packets", value: "↑↑", why: "auto-computed = high" },
      { feature: "psh_flag_count", value: "5", why: "pushes the malicious payload" },
      { feature: "dst_port", value: "80 / 443", why: "the web tier" },
      { feature: "flow_duration", value: "1800 μs", why: "long-ish single HTTP request" },
    ],
    dst_port_default: 443,
    flow: {
      protocol: "TCP",
      flow_duration: 1800,
      total_fwd_packets: 10,
      total_bwd_packets: 8,
      flow_packets_per_s: 7,
      flow_bytes_per_s: 12000,
      fwd_packet_length_max: 1200,
      fwd_packet_length_mean: 800,
      bwd_packet_length_max: 900,
      bwd_packet_length_mean: 500,
      flow_iat_mean: 400,
      flow_iat_std: 250,
      fin_flag_count: 1,
      syn_flag_count: 1,
      rst_flag_count: 0,
      psh_flag_count: 5,
      ack_flag_count: 5,
      src_ip: "203.0.113.7",
      dst_ip: "10.0.0.80",
    },
  },
  {
    name: "Botnet",
    tag: "C2 beaconing",
    why_short: "very long-lived, low-rate flow with suspiciously regular timing.",
    why_full:
      "A compromised host phones home on a schedule. The flow lives for tens of milliseconds, the byte-rate is tiny, but the giveaway is timing: flow_iat_std is far below what real human traffic produces. Beacons are too regular to be a person at a keyboard.",
    signals: [
      { feature: "flow_duration", value: "162 000 μs (162 ms)", why: "long-lived sessions" },
      { feature: "flow_iat_std", value: "2.7", why: "LOW — beacons fire on a timer" },
      { feature: "flow_iat_mean", value: "2980 μs", why: "the beacon period itself" },
      { feature: "flow_bytes_per_s", value: "55", why: "almost nothing — tiny heartbeats" },
      { feature: "total_fwd / total_bwd", value: "5 / 5", why: "symmetric request/reply" },
    ],
    dst_port_default: 8080,
    flow: {
      protocol: "TCP",
      flow_duration: 162000,
      total_fwd_packets: 5,
      total_bwd_packets: 5,
      flow_packets_per_s: 1.6,
      flow_bytes_per_s: 55,
      fwd_packet_length_max: 150,
      fwd_packet_length_mean: 120,
      bwd_packet_length_max: 150,
      bwd_packet_length_mean: 120,
      flow_iat_mean: 2980,
      flow_iat_std: 2.7,
      fin_flag_count: 0,
      syn_flag_count: 1,
      rst_flag_count: 0,
      psh_flag_count: 1,
      ack_flag_count: 3,
      src_ip: "10.0.0.42",
      dst_ip: "185.220.101.7",
    },
  },
  {
    name: "Infiltration",
    tag: "exfiltration",
    why_short: "looks benign per-packet but moves an unusual amount of data outward.",
    why_full:
      "An internal host quietly uploads sensitive data over an established session. Each packet looks normal in isolation, but the aggregate outbound payload is far above what a user of that role would generate. The model latches on to total_length_fwd_packets and flow_bytes_per_s, which both sit well above the benign baseline.",
    signals: [
      { feature: "total_length_fwd_packets", value: "↑↑↑", why: "auto from mean × count — the giveaway" },
      { feature: "flow_bytes_per_s", value: "13 360", why: "high — sustained upload" },
      { feature: "flow_duration", value: "60 000 μs", why: "long-lived transfer" },
      { feature: "fwd_packet_length_mean", value: "~1400 B", why: "near-MTU packets full of data" },
      { feature: "rst / fin counts", value: "near 0", why: "session held open the whole time" },
    ],
    dst_port_default: 443,
    flow: {
      protocol: "TCP",
      flow_duration: 60000,
      total_fwd_packets: 15,
      total_bwd_packets: 8,
      flow_packets_per_s: 7,
      flow_bytes_per_s: 13360,
      fwd_packet_length_max: 1500,
      fwd_packet_length_mean: 1400,
      bwd_packet_length_max: 200,
      bwd_packet_length_mean: 80,
      flow_iat_mean: 400,
      flow_iat_std: 250,
      fin_flag_count: 0,
      syn_flag_count: 1,
      rst_flag_count: 0,
      psh_flag_count: 4,
      ack_flag_count: 7,
      src_ip: "10.0.0.99",
      dst_ip: "185.220.101.42",
    },
  },
];

export function findExample(name: string): Example | undefined {
  return EXAMPLES.find((e) => e.name === name);
}
